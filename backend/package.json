{
    "name": "backend",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "tsx watch --env-file=.env src/index.ts",
        "build": "tsc",
        "start": "node --env-file=.env dist/index.js",
        "format": "prettier --write .",
        "db:dev": "source .env && psql postgres://postgres:postgres@localhost:5432/postgres -c \"DROP DATABASE IF EXISTS $DATABASE_NAME WITH (FORCE)\" && psql postgres://postgres:postgres@localhost:5432/postgres -c \"CREATE DATABASE $DATABASE_NAME\" && psql postgres://postgres:postgres@localhost:5432/$DATABASE_NAME -f migrations.sql"
    },
    "dependencies": {
        "pg": "^8.13.3",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@types/node": "^22.13.10",
        "@types/pg": "^8.11.11",
        "prettier": "3.8.1",
        "tsx": "^4.19.3",
        "typescript": "^5.8.2"
    }
}
